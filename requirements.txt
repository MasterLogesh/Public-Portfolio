Django>=4.0
gunicorn
psycopg2-binary
django-crispy-forms
django-widget-tweaks
whitenoise
python-dotenv

If you're using SQLite locally and PostgreSQL for deployment (like on Heroku/Render), psycopg2-binary is recommended.
